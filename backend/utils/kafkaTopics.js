const kafkaTopics = {
  PHOTO_UPLOADS: "photo-uploads",
  EVENT_UPDATES: "event-updates",
  PHOTO_DLQ: "photo-dlq",
};

export default kafkaTopics;
export const { PHOTO_UPLOADS, EVENT_UPDATES, PHOTO_DLQ } = kafkaTopics;
